fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
geoalchemy2==0.14.3
pydantic==2.10.6
pydantic-settings==2.7.1
email-validator==2.1.0
PyJWT[crypto]==2.13.0
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.32
celery==5.3.6
redis==7.3.0
httpx==0.28.1
google-cloud-aiplatform==1.141.0
google-cloud-kms==2.21.0
aiohttp==3.14.1
google-auth==2.47.0
google-cloud-secret-manager==2.18.0
google-cloud-iam==2.14.0
alembic==1.13.1
aiofiles==23.2.1
cryptography==49.0.0
slowapi==0.1.9
boto3==1.34.25
sentry-sdk[fastapi]==2.54.0

# Content moderation for public inputs (profanity/explicit text detection)
better-profanity==0.7.0

# Sentiment analysis for the research portal. VADER is a published, validated
# rule-based model (MIT, pure Python, no model download) that handles negation
# and intensifiers. Optional: research exports fall back to a keyword scorer.
vaderSentiment==3.3.2

# Face / licence-plate redaction of resident photos, applied destructively at
# intake. Pillow does the blur and the re-encode (which also drops the EXIF GPS
# block a phone attaches). opencv-python-headless is the offline, no-cost
# detector -- headless because the GUI build pulls in X11 for imshow() that a
# container will never call. Both are optional at runtime: without them
# image_redaction degrades to storing the photo as submitted.
Pillow==11.1.0
opencv-python-headless==4.11.0.86

# The free plate detector. Tesseract is Apache 2.0, which is what makes it
# usable here -- OpenALPR and every off-the-shelf Ultralytics YOLO plate model
# are AGPL-3.0, and that clause would attach to every town that self-hosts and
# exposes this over a network. pytesseract needs the `tesseract-ocr` system
# binary (installed in the Dockerfile); without it the plate text signal simply
# turns off and face redaction is unaffected.
pytesseract==0.3.13
